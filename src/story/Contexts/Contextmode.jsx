import { createContext } from "react";
export const ContextColormode=createContext({//This is code for colormode (dark and light)
    toggelColor:()=>{}
});
export const GlobalContexts=createContext({
    menuToggel:()=>{}//This is code for button menu
});
